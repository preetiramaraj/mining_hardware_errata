run find_trigrams.py as


find_trigrams.py > a.txt

in order to get the top frequent trigrams in the failure and workaround section corresponding to each core. Have omitted the top 80 words for workaround since they are majorly influenced by the header values. The values can be changed accordingly in the code.
